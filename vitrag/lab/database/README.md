# Database — Postgres for the lab
# Intentionally weak config (default password, exposed port).
# No Dockerfile needed — we use the official image directly in docker-compose.
# This file is here for reference / documentation.
